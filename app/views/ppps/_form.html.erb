<%= form_for(@ppp) do |f| %>
  <% if @ppp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ppp.errors.count, "error") %> prohibited this ppp from being saved:</h2>

      <ul>
      <% @ppp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
